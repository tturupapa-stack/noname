"당신이 잠든 사이" 프로젝트에 Slack 웹훅 알림 기능을 추가해줘.

## 목적
매일 아침 리포트 생성이 완료되면 Slack 채널로 요약 알림을 보내는 API

## 구현 요구사항

1. **API 엔드포인트**: POST /api/notifications/slack
   - 리포트 데이터를 받아서 Slack으로 전송
   - 요청 파라미터: report_date, summary(수익률, 주요 이벤트 등), report_url

2. **Slack 연동**
   - Incoming Webhook 방식 사용
   - SLACK_WEBHOOK_URL 환경변수로 관리 (.env에 추가)
   - Block Kit 포맷으로 가독성 좋은 메시지 구성

3. **메시지 포맷**
   - 헤더: "📊 당신이 잠든 사이 - Daily Report"
   - 본문: 날짜, 총 수익률, 주요 알림 사항
   - 하단: 대시보드 링크 버튼

4. **에러 핸들링**
   - Webhook 실패 시 최대 3회 재시도
   - 성공/실패 로깅

5. **기존 리포트 생성 로직과 연동**
   - 리포트 생성 완료 시점에 이 API를 호출하도록 연결

먼저 현재 프로젝트 구조를 파악하고, 기존 코드 스타일에 맞춰서 구현해줘.